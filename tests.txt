//Basic Command Parsing
echo hello

//pipes
ls -l | grep "main"

//Multiple Pipes with Quotes
cat "some file" | grep "hello $USER" | wc -l

//Single Quotes — No Expansion
echo '$HOME is cool'

//Double Quotes — With Expansion
echo "$HOME is cool"

//Mixed Quotes
echo 'user:' "$USER"

//Variables Alone
echo $PATH

//syntax error
echo "hello

//empty pipes
| ls
ls |
ls || echo hi
ls | | grep                    # invalid
| echo hello                   # invalid
echo hello || wc               # invalid
echo hello | wc                # valid
echo "hello | world" | wc      # valid
echo '|'                       # valid
cat << EOF | wc                # valid


//quotes with pipes
echo "this | is not a pipe"

// quotes
"hello"
'world'
"hello" 'world""'

//empty input
parser returns NULL or t_cmd with NULL args